import React from "react";
import AppNavigator from "./navigation/AppNavigator";

// This is the main entry point of the application. It imports the AppNavigator component from the navigation folder and renders it as the root component of the app.
export default function App() {
  return <AppNavigator />;
}
